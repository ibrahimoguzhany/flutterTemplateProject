import 'package:flutter/material.dart';

class UnPlannedTourListView extends StatelessWidget {
  const UnPlannedTourListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Plansız Turlar"),
      ),
    );
  }
}
