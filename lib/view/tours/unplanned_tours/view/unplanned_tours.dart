import 'package:flutter/material.dart';

class UnPlannedToursView extends StatelessWidget {
  const UnPlannedToursView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Plansız Turlar"),
      ),
    );
  }
}
